; ==========================================================
; AMX Mod X 1.10.0 (HL25-compatible) - plugins loaded at startup
; ==========================================================
; Fixed: alliedmodders/amxmodx#1086 - no more segfault with HLDS 10211.
; ALL modules (cstrike, hamsandwich, fun) now work with ReGameDLL 5.28.
;
; In-game admin menu: amx_menu  |  Player help: /help
; Bind a key: bind v "amx_menu"

; --- Core admin ---
admin.amxx
admincmd.amxx
adminhelp.amxx
adminslots.amxx
multilingual.amxx

; --- Menus (1-9 keys, 0 = back) ---
; menufront.amxx         ; disabled: renders incorrectly (shows only "0 Exit"), replaced by admin_menu.amxx
admin_menu.amxx          ; custom replacement: clean tree menu via amx_menu / /admin
cmdmenu.amxx
plmenu.amxx
mapsmenu.amxx
; pluginmenu.amxx         ; disabled: requires menufront.amxx which is replaced by admin_menu

; --- Chat / voting ---
adminchat.amxx
antiflood.amxx
scrollmsg.amxx
adminvote.amxx

; --- Map management ---
nextmap.amxx
mapchooser.amxx
timeleft.amxx

; --- Runtime config ---
pausecfg.amxx
statscfg.amxx

; --- Counter-Strike (cstrike addon) ---
restmenu.amxx
statsx.amxx
miscstats.amxx
stats_logging.amxx

; --- Custom plugins ---
full_equip.amxx         ; max money + kevlar+helmet on spawn (fe_money, fe_armor)
server_tools.amxx       ; /revive /respawn + amx_revive for admins
fun_extras.amxx         ; /rtv, /help, welcome
map_groups.amxx         ; /maps /de /cs /fy /aim /awp /rats /ka
weapon_limits.amxx      ; AWP limit + auto sniper ban (wl_*)
loadout_packs.amxx      ; /packs spawn loadouts: choose once, auto-equip every spawn
defibrillator.amxx      ; /defib /medic /heal — players revive teammates (df_*)
bot_auto_replace.amxx   ; same-team bot kick when a human picks T or CT
; bot_manager.amxx      ; removed: native CZ bots via bot_quota (ReGameDLL) replaces this
ai_commentator.amxx     ; AI game commentator (Cloudflare Workers AI / Gemma 4)
